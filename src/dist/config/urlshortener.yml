server:
  applicationConnectors:
    - type: http
      port: 8888

redis:
  host: localhost
  port: 6379

logging:
  level: INFO
  loggers:
    com.urlshortener: DEBUG

  appenders:
    - type: console
